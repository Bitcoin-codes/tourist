node_modules/
__pycache__/
*.pyc
.DS_Store

# Secrets — SMTP / Supabase / any API credentials
.env
.env.*
!.env.example

# Supabase CLI local state (contains DB connection strings)
supabase/.temp/
**/.supabase/
.vercel
.env*
# The template carries setup notes (NOTIFY_EMAIL) and no secrets — only the
# real .env stays out.
!.env.example

# Unreferenced screenshot artifact
hero-preview.png
